html
  head
    title= MiniProject
    script(src='/index.js')
    style.
      #container {
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        top: 30%;
        width: 50%;
      }
      *:focus {
        outline: none;
      }

      button {
        position: fixed;
        width: 40px;
        height: 40px;
        background-color: white;
        border-radius: 20px;
        border: solid 1px black;
        right: 0px;
      }

      button:hover, .hover {
        background-color: black;
      }

      h1 {
        margin: 5px;
      }

      li {
        list-style-type: none;
        translateX: -3px;
        padding-left: 5px;
      }

      li:last-child{
        margin-bottom: 5px;
      }

      h3 {
        margin-bottom: 10px;
        margin-top: 15px;
      }

      li:hover {
        box-shadow: -3px 0 0 black;
      }

      p {
        margin: 0px;
      }

      input {
        font-size: 15px;
        border-radius: 20px;
        border: solid 1px black;
        padding: 15px;
        width: calc(100% - 45px);
        height: 40px;
      }

      blockquote {
        color: grey;
        font-size: 12px;
        text-align: center;
        margin: 30px;
      }

  body
    #container
      h1 Mini Project
      input#address(type='text' placeholder='구 또는 동 단위 까지 입력해주세요 (예: 강원도 춘천시 석사동, 서울특별시 마포구)' )
      button#search
      ui#result
      blockquote 공적 마스크 판매정보 api를 사용한 주소 기준 동네별 공적 마스크 판매정보 제공 서비스
